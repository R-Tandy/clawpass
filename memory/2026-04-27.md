# 2026-04-27

## ClawPass Sync Investigation

### Desktop rebuild (12:15-12:23 CDT)
- Rewrote `sync_tcp.rs` with proper error logging
- Added JSON parse error logging: `[SyncServer] JSON parse error: {e} | bytes: {raw}`
- Changed log level to INFO for raw bytes visibility
- Desktop app restarted multiple times — running fresh build

### Last sync attempt (12:17-12:18)
- iOS connected from 192.168.4.60:54202 at 17:18:05
- Desktop read error: 10054 (forcibly closed by remote)
- NO "Received raw bytes" log → desktop app wasn't rebuilt yet when this happened

### Current state
- Desktop: fresh build with debug logging, running since ~17:23 CDT
- iOS: old build (before `b112e54` domain fix) — won't discover via Bonjour
- iOS CI: has NOT been rebuilt with `b112e54` yet

### Tonight's testing checklist
1. Check GitHub Actions for iOS build status (was building when we stopped)
2. Pull latest iOS code, verify build
3. Install new iOS build
4. Test sync:
   - Check desktop logs for "Received raw bytes"
   - If JSON parse error → iOS is sending unexpected format
   - If no logs → connection not reaching desktop (firewall/Bonjour)
5. Try manual TCP connect (IP:7878) as backup

### iOS says "sync completed" but desktop vault unchanged
- Suggests iOS got a response it accepted (maybe old `FullSync` type string matching?)
- But desktop's `serde_json::from_slice` failed silently (no `else` clause)
- FIX: added explicit error logging

### Protocol status
- Desktop sends: `{"type":"sync_response","entries":[...],"timestamp":...}`
- iOS expects: `{"type":"sync_response","entries":[...],"timestamp":...}` (same)
- But old desktop build was sending `type:"FullSync"` with `#[serde(tag)]` internal format
- New build uses `#[serde(rename = "sync_response")]` — should be correct

### Console popup windows
- NOT from app's `println!` — all replaced with `tracing!`
- mDNSResponder.exe running (Windows Bonjour service)
- Appears every ~10 seconds — likely Windows scheduling DNS cache refresh
- NOT YET confirmed fixed — needs evening test

### Next steps tonight
1. `git pull` both desktop and iOS
2. Verify desktop build is clean
3. Check if iOS CI built the `b112e54` commit
4. Install new iOS build, test Bonjour discovery
5. If no discovery, try manual IP connect
