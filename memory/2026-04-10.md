# 2026-04-10 - ClawPass Fixes

## Fixed Today
- **Critical bug**: `save_vault_to_disk` in commands.rs was using `VAULT_FILE` constant instead of `get_vault_path()`. This caused entries to not persist because the vault was being saved to the wrong directory.
  - Changed: `fs::write(VAULT_FILE, &encrypted)` → `fs::write(get_vault_path()?, &encrypted)`
  - This should fix the "entries disappear after save" issue.

## Created
- `C:\Users\Reno\Documents\password-manager\ClawPass\TODO.md` with comprehensive status and remaining issues.

## Notes
- Reno wants to continue with ClawPass fixes when ready.
- Gateway config for LM Studio still has issues (separate from ClawPass).
