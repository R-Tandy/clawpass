# 2026-04-28 - Daily Memory

## Desktop App Issues (reported ~01:21 CDT)
- **Categories messed up** - desktop Vue app categories not displaying correctly
- **Favorites not showing in list** - favorites filter not working properly

## Desktop VaultView.vue Architecture (from context)
- Sidebar: "All Items" (no selectedCategory), "Favorites" (selectedCategory = 'favorites'), then user categories
- Entry list: `v-for="entry in vault.filteredEntries"` with `:class="{ favorite: entry.is_favorite }"`
- `filteredEntries` computed property filters by `selectedCategory` - if 'favorites', shows only `entry.is_favorite` entries
- Categories stored in `vault.categories` array with `{ id, name, icon }`

## Sync Protocol Status
- iOS ↔ Desktop TCP sync on port 7878 working (handshake + FullSync confirmed)
- Entries merging but NOT appearing in iOS vault list (UI issue, not protocol)
- Desktop sending entries but Reno reports desktop also has display issues

## iOS Build #46 (73482aa)
- Status display time extended to 4 seconds
- Success message shows checkmark suffix

## Session End (23:15 CDT)
- Out of tokens - Reno AFK for ~4 days

## Previous Memory Files
- `memory/2026-04-27.md` - ClawPass sync debugging details
- `memory/2026-04-26.md` - Categories and VaultManager context
