
## 2026-05-06 03:42 CDT
- Fixed the Vue runtime error `v-else/v-else-if has no adjacent v-if` in `EntryDetailModal.vue`.
- Replaced all stray `v-else` directives with `v-show="editMode"` (or appropriate `v-show` conditions) for edit fields (username, password, URL, notes) and the eye‑icon toggle.
- Updated template to use `v-show` consistently, eliminating the warning and allowing the entry‑detail modal to render correctly.
- Confirmed the UnlockView component has no v-else issues.
- Added a note that the error is resolved and the UI functions as intended.
